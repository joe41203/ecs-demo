[
  {
    "name": "nginx",
    "image": "${image}",
    "memory": 200,
    "cpu": 10,
    "essential": true,
    "portMappings": [
      {
        "containerPort": 80,
        "protocol": "tcp",
        "hostPort": 80
      }
    ],
    "logConfiguration":{
          "logDriver":"awslogs",
          "options":{
             "awslogs-group":"awslogs-nginx-ecs",
             "awslogs-region":"ap-northeast-1",
             "awslogs-stream-prefix":"ecs"
          }
    }
  }
]
